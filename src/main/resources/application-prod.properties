server.servlet.context-path=/emr
server.servlet.session.timeout=15m

spring.servlet.multipart.enabled=true
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

file.upload-dir=/myprojects/uploads

# Spring Data REST
spring.data.rest.base-path=/api
spring.data.rest.detection-strategy=annotated

# PROD settings
spring.datasource.initialization-mode=never
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# MySQL DataSource config
# spring.datasource.url=jdbc:mysql://localhost:3306/cdsiemr
# spring.datasource.username=hpaatsmy_dbmgr
# spring.datasource.password=Pinoytasty103!

# MySQL DataSource config using Railway Variables
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}

